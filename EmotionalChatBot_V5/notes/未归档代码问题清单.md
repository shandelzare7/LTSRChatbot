# 未归档代码问题清单

基于当前 git 未提交/未跟踪的代码与配置的审查，按文件与类别整理。供后续逐项修复或归档决策。

---

## 一、graph.py

| # | 问题 | 位置/说明 | 状态 |
|---|------|-----------|------|
| 1 | **重复导入** `from pathlib import Path` | 第 8 行与第 18 行各一次，可删其一。 | ✅ 已删第 18 行重复导入 |
| 2 | **未使用变量** `root = get_project_root()` | 第 82 行赋值后未使用，可删除或改为后续用到（如路径拼接）时再取。 | ✅ 已删除 root 及 get_project_root 导入 |
| 3 | **大量 get_llm() 调用** | 每节点一个 LLM 实例，若 role 相同仅 temperature 不同会创建多实例，可能增加连接/内存；可考虑按 (role, temperature) 缓存。 | ✅ 已在 app/services/llm.py 中按 (role, model_name, base_url, temperature) 缓存 |

---

## 二、strategy_routers.py

| # | 问题 | 位置/说明 | 状态 |
|---|------|-----------|------|
| 4 | **硬规则里的「≥0.8」** | 第 196 行写「只有在你确信 ≥0.8 时才输出 id」——模型无法真的输出置信度，易被理解为模糊约束；若仅为语气强调可保留，否则可改为「仅当非常确定时」。 | ✅ 已改为「仅当非常确定命中时才输出 id」 |
| 5 | **BUSYNESS_THRESHOLD 重复定义** | 本文件第 29 行与 strategy_resolver.py 第 55 行均为 `0.6`，应抽到共用常量或单一 config，避免两处改不同步。 | ✅ resolver 改为从 strategy_routers 引入，单源 |
| 6 | **背景信息截断只用前 300/400/600** | user_content 里 `rel_desc[:600]`、`stage_desc[:400]`、bot/user `[:300]` 为截「开头」；若这些字段本身是「越靠前越重要」则合理，否则与 recent_dialogue 的「保留结尾」策略不一致，需统一约定。 | ✅ 已加注释约定：摘要字段取前 N 字，与聊天记录保留结尾区分 |

---

## 三、strategy_resolver.py

| # | 问题 | 位置/说明 | 状态 |
|---|------|-----------|------|
| 7 | **BUSYNESS_THRESHOLD 与 routers 重复** | 见上第 5 条。 | ✅ 已从 strategy_routers 引入并统一 |
| 8 | **Deferral_Score 阈值 0.5 为魔法数字** | 第 201 行 `deferral_score > 0.5` 触发 busy_deferral，建议抽成配置或命名常量并注释含义。 | ✅ 已抽成 DEFERRAL_SCORE_THRESHOLD = 0.5 |
| 9 | **STRATEGY_PRIORITY_13 与三 router 的 id 集合可能不同步** | 若 strategies.yaml 或 router 的 HIGH_STAKES_IDS 等增删 id，此处列表需人工同步，易漏；可考虑从配置生成或启动时校验。 | ✅ 启动时 _assert_router_ids_in_priority() 校验三路 id 均在优先级列表中 |

---

## 四、常量与魔法数字（多文件）

| # | 问题 | 位置/说明 |
|---|------|-----------|
| 10 | **0.8 / 0.3 / 30 等分散** | processor.py、style.py、memory_manager.py、lats_search.py、evolver.py 等大量 0.8、0.3、30 等未命名常量，不利于调参与文档化；建议关键业务阈值抽到 config 或模块级常量并命名。 |
| 11 | **typing_speed 范围 0.5~30.0** | processor.py 第 265 行，单位与含义未在注释说明（秒？字符/秒？），易误用。 |
| 12 | **macro_delay > 300.0 为 idle** | processor.py 第 272 行，300 单位未写（毫秒？），与 typing_speed 等单位约定宜统一文档。 |

---

## 五、state.py / 类型与契约

| # | 问题 | 位置/说明 | 状态 |
|---|------|-----------|------|
| 13 | **> 0.8 强制缩短回复** | 第 130 行注释提到「> 0.8 时会强制缩短回复」，若对应逻辑在别处，state 里应有字段或指针说明；若已移除，注释应删。 | ✅ 已修正注释：代码中无「强制缩短回复」逻辑；busyness 仅用于 routers/resolver「忙碌」判定与 processor 延迟。 |
| 14 | **TypedDict total=False 下键可选** | AgentState 等大量可选键，调用方若假定某键存在会静默出错；关键路径建议在节点入口做必要键校验或文档明确「谁在何时写入」。 | ✅ 已在 AgentState docstring 中增加「关键字段写入者/时机」列表，并提醒用 .get() 或入口校验。 |

### 13、14 详细核查结论

**13（busyness 与「强制缩短回复」）**  
- 全文检索：没有任何一处实现「busyness > 0.8 时强制缩短回复」。  
- 实际用法：`strategy_routers` / `strategy_resolver` 用 `busyness >= 0.6` 判「忙碌」并参与 deferral；`processor` 在 `busyness > 0.85` 时以 70% 概率加长宏观延迟（模拟忙碌）。  
- task_planner 的 prompt 里虽有「word_budget 通常不超过 30/20」及 mood_state，但当前节点已注释掉 LLM 预算规划，返回值中**不包含 word_budget**；requirements 也未使用 word_budget。  
- 结论：原注释为过时设计或未实现逻辑，已改为如实描述当前下游用途。

**14（AgentState 可选键与契约）**  
- `AgentState(TypedDict, total=False)` 导致所有键可选，若用 `state["key"]` 可能 KeyError。  
- 已在 AgentState 的 docstring 中补充：  
  - 标明「全部键可选」；  
  - 列出「关键字段写入者/时机」（loader / detection / routers / strategy_resolver / style / task_planner / lats_search / processor / evolver / memory_manager）；  
  - 建议调用方使用 `.get()` 或在节点入口做必要键校验。

---

## 六、lats / reply_planner / evaluator

| # | 问题 | 位置/说明 | 状态 |
|---|------|-----------|------|
| 15 | **early_exit 阈值 0.82 / 0.80** | lats_search.py 第 80–81 行 `lats_early_exit_root_score`、`lats_early_exit_plan_alignment_min` 为魔法数字，建议进 config 或常量。 | ✅ 已抽成模块常量 LATS_EARLY_EXIT_*_DEFAULT。 |
| 16 | **_planner_sampling_for_round 与 graph 的 temperature** | reply_planner 内 gen_round=0 时 temperature=0.7，graph 又传 llm_planner_27 temperature=0.7；invoke 时再传 temperature 会覆盖，两处若以后改不同会困惑，建议注释写明「以 invoke 传入为准」或只保留一处配置。 | ✅ 已在 reply_planner 与 graph 注释中写明：实际以 invoke 传入（_planner_sampling_for_round）为准。 |

---

## 七、config / yaml

| # | 问题 | 位置/说明 | 状态 |
|---|------|-----------|------|
| 17 | **momentum_formula / stages / strategies 与代码强绑定** | 改 yaml 未改代码会导致行为不一致；建议在文档或启动时做 schema/引用校验。 | ✅ 已确认强绑定；已新增 config/README.md 列出各 yaml 消费者与约定，strategies 有 _assert_router_ids_in_priority 启动校验。 |
| 18 | **content_moves.yaml 与 reply_planner 约定** | LATS 27 候选依赖 8 条 move，应归档并注明约定。 | ✅ 已在 config/README.md 中注明 content_moves 与 plan_reply_27_via_content_moves 的约定；文件应纳入版本控制。 |

---

## 八、其他

| # | 问题 | 位置/说明 | 状态 |
|---|------|-----------|------|
| 19 | **detection 用 importlib 动态加载** | graph.py 因与 detection 目录同名而绕开正常 import，可维护性差。 | ✅ 已改为普通 import：`from app.nodes.detection import create_detection_node`。 |
| 20 | **print 调试输出** | strategy_resolver、strategy_routers、lats_search 等多处 print，生产环境宜用 logger。 | ✅ 已改为 logging.getLogger(__name__)，print 改为 logger.info / logger.warning。 |

---

## 摘要（前 20 条）

1. graph.py：Path 重复导入；root 未使用；多 LLM 实例可考虑缓存。  
2. strategy_routers：硬规则「≥0.8」语义模糊；BUSYNESS_THRESHOLD 与 resolver 重复；背景信息截断策略与 recent_dialogue 不一致。  
3. strategy_resolver：BUSYNESS_THRESHOLD 重复；Deferral_Score 0.5 魔法数字；STRATEGY_PRIORITY_13 与 router/配置可能不同步。  
4. 多文件：0.8/0.3/30 等魔法数字；processor 中 typing_speed、macro_delay 单位未文档化。  
5. state：强制缩短回复注释与实现是否一致；TypedDict 可选键易导致静默错误。  
6. lats：early_exit 阈值未配置化；reply_planner temperature 两处配置易混淆。  
7. config：yaml 与代码绑定未校验；content_moves.yaml 未归档。  
8. 其他：detection 动态加载可维护性差；print 应改为 logger。

---

*生成说明：本清单由未归档代码审查生成（含 Cursor 本地审查）。若已用「claude code」在终端跑过完整审查，可对比其输出做补充或合并。*
